{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "iverilog",
            "type": "shell",
            "command": "iverilog -o ./bin/out ./src/tb.v; vvp ./bin/out",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}